#!/bin/bash
timeout=10m
if [[ "$1" = -* || "$1" = "" ]]; then
  t=""
else
  t="$1"
  shift
fi
set -x

cd -P .
cpulimit -l 600 -q -- make test PKG=./pkg/sql/opt/exec/execbuilder TESTS="$t" TESTFLAGS="$*" TESTTIMEOUT=5s |& tee /tmp/log

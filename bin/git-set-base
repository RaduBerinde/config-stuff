#!/bin/bash

# . git-common-stuff.sh
#
# CUR=`get_current_branch`

UP="$1"
[ -z "$1" ] && UP="master"

#
# if [ -z "$CUR" ]; then
#    echo "Not on a branch"
#    exit 1
# fi
#
#
# git checkout $UP && git branch --set-upstream $CUR && git checkout $CUR
#
git branch --set-upstream-to=$UP
